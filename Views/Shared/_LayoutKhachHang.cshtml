<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - HUFLIT HOTEL</title>

    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

    <!-- Font & Icon -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
    

    <style>
        /* ==== HEADER NAVBAR ==== */
        .header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 1000;
            background: transparent;
            padding: 20px 0;
            transition: background 0.4s ease, box-shadow 0.4s ease;
        }

        .header-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo a {
            font-size: 24px;
            font-weight: 700;
            text-decoration: none;
            color: #fff;
            letter-spacing: 1px;
            transition: color 0.4s;
        }

        .main-nav ul {
            display: flex;
            list-style: none;
            gap: 30px;
            margin: 0;
            padding: 0;
        }

            .main-nav ul li {
                display: inline-block;
            }

                .main-nav ul li a {
                    display: inline-block;
                    text-decoration: none;
                    color: #fff;
                    font-weight: 500;
                    transition: color 0.3s;
                }

                    .main-nav ul li a:hover {
                        color: #ffd700;
                    }


        .header-actions .btn-outline-light {
            color: #fff;
            border-color: #fff;
            transition: all 0.3s;
        }

            .header-actions .btn-outline-light:hover {
                background: #fff;
                color: #333;
            }

        /* ==== SCROLLED STATE ==== */
        .header.scrolled {
            background: #fff;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
            padding: 10px 0;
        }

            .header.scrolled .logo a,
            .header.scrolled .main-nav ul li a,
            .header.scrolled .header-actions .text-white {
                color: #333 !important;
            }

            .header.scrolled .btn-outline-light {
                color: #333;
                border-color: #333;
            }

                .header.scrolled .btn-outline-light:hover {
                    background: #333;
                    color: #fff;
                }

        /* Đẩy nội dung xuống tránh bị navbar che */
        .body-content {
            padding-top: 100px;
        }

        /* ==== FOOTER ==== */
        .footer {
            background: #111; /* nền đen sang trọng */
            color: #eee;
            padding: 60px 0 30px 0;
            font-family: 'Roboto', sans-serif;
        }

            .footer .footer-content {
                display: flex;
                flex-wrap: wrap;
                justify-content: space-between;
                gap: 30px;
            }

        .footer-logo a {
            font-size: 24px;
            font-weight: 700;
            text-decoration: none;
            color: #fff;
            display: inline-block;
            margin-bottom: 10px;
            margin-left: 20px;
        }

        .footer-logo p {
            font-size: 14px;
            color: #bbb;
            max-width: 250px;
            margin-left: 20px;
        }

        .footer-links h4 {
            font-size: 16px;
            font-weight: 600;
            color: #d4af37;
            margin-bottom: 15px;
            position: relative;
            display: inline-block;
        }

            .footer-links h4::after {
                content: '';
                display: block;
                width: 30px;
                height: 2px;
                background: #d4af37;
                margin-top: 5px;
            }

        .footer-links ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }

            .footer-links ul li {
                margin-bottom: 8px;
            }

                .footer-links ul li a {
                    text-decoration: none;
                    color: #ccc;
                    font-size: 14px;
                    transition: color 0.3s, transform 0.3s;
                    display: inline-block;
                }

                    .footer-links ul li a:hover {
                        color: #fff;
                        transform: translateX(5px);
                    }

        .footer-social {
            text-align: center;
            margin-top: 40px;
            margin-right: 20px;
        }

            .footer-social h4 {
                color: #d4af37;
                font-size: 16px;
                font-weight: 600;
                margin-bottom: 15px;

            }

        .social-icons a {
            display: inline-block;
            margin: 0 8px;
            font-size: 18px;
            color: #ccc;
            transition: color 0.3s, transform 0.3s;
        }

            .social-icons a:hover {
                color: #d4af37;
                transform: translateY(-3px);
            }

        .footer-bottom {
            border-top: 1px solid #333;
            margin-top: 30px;
            padding-top: 15px;
            text-align: center;
            font-size: 14px;
            color: #777;
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <header class="header">
        <div class="header-container">
            <!-- Logo -->
            <div class="logo">
                <a href="@Url.Action("Index", "Home")">HUFLIT HOTEL</a>
            </div>

            <!-- Desktop Navigation -->
            <nav class="main-nav">
                <ul>
                    <li>@Html.ActionLink("Trang chủ", "TrangChuNguoiDung", "Home")</li>
                    <li>@Html.ActionLink("Phòng & Suite", "RoomsAndSuite", "Home")</li>
                    <li>@Html.ActionLink("Ẩm thực", "Dining", "Home")</li>
                </ul>
            </nav>

            <!-- Auth & Book Now Buttons -->
            <div class="header-actions">
                <div class="d-flex align-items-center gap-2">
                    @if (Session["UserId"] != null)
                    {
                        <span class="text-white me-2">
                            Xin chào, <strong>@Session["DisplayName"]</strong>
                        </span>

                        <a href="@Url.Action("Logout", "Auth")" class="btn btn-outline-light" title="Đăng xuất">
                            <i class="bi bi-box-arrow-right"></i>
                        </a>
                    }
                    else
                    {
                        <a href="@Url.Action("Login", "Auth")" class="btn btn-outline-light">
                            Đăng nhập
                        </a>
                    }

                    @if (Session["RoleName"] != null && Session["RoleName"].ToString() == "Khách hàng")
                    {
                        <button type="button" class="btn btn-warning" data-bs-toggle="modal" data-bs-target="#customerModal" title="Nhập thông tin khách hàng">
                            <i class="bi bi-person-plus-fill"></i>
                        </button>
                        <button type="button" class="btn btn-warning" data-bs-toggle="modal" data-bs-target="#bookingModal">
                            ĐẶT PHÒNG NGAY
                        </button>
                    }
                </div>
            </div>
        </div>
    </header>

    <div class="container body-content">
        @RenderBody()
    </div>

    <!-- Footer -->
    <footer class="footer mt-5">
        <div class="container">
            <div class="footer-content">
                <div class="footer-logo">
                    <a href="@Url.Action("Index", "Home")">HUFLIT HOTEL</a>
                    <p>Trải nghiệm đỉnh cao của sự sang trọng và hiếu khách</p>
                </div>

                <div class="footer-links row">
                    <div class="footer-column col-md-4">
                        <h4>Khám phá</h4>
                        <ul>
                            <li>@Html.ActionLink("Trang chủ", "Index", "Home")</li>
                            <li><a href="#">Phòng & Suite</a></li>
                            <li><a href="#">Ẩm thực</a></li>
                            <li><a href="#">Trải nghiệm</a></li>
                        </ul>
                    </div>

                    <div class="footer-column col-md-4">
                        <h4>Thông tin</h4>
                        <ul>
                            <li>@Html.ActionLink("Về chúng tôi", "About", "Home")</li>
                            <li><a href="#">Thư viện ảnh</a></li>
                            <li>@Html.ActionLink("Liên hệ", "Contact", "Home")</li>
                            <li><a href="#">Câu hỏi thường gặp</a></li>
                        </ul>
                    </div>

                    <div class="footer-column col-md-4">
                        <h4>Điều khoản</h4>
                        <ul>
                            <li><a href="#">Chính sách bảo mật</a></li>
                            <li><a href="#">Điều khoản & điều kiện</a></li>
                            <li><a href="#">Chính sách hủy phòng</a></li>
                        </ul>
                    </div>
                </div>

                <div class="footer-social mt-4">
                    <h4>Theo dõi chúng tôi</h4>
                    <div class="social-icons">
                        <a href="#"><i class="fab fa-facebook-f"></i></a>
                        <a href="#"><i class="fab fa-instagram"></i></a>
                        <a href="#"><i class="fab fa-twitter"></i></a>
                        <a href="#"><i class="fab fa-linkedin-in"></i></a>
                    </div>
                </div>
            </div>

            <div class="footer-bottom text-center mt-4">
                <p>&copy; @DateTime.Now.Year HUFLIT HOTEL. Mọi quyền được bảo lưu.</p>
            </div>
        </div>
    </footer>

    <!-- Scroll Script -->
    <script>
        window.addEventListener("scroll", function () {
            const header = document.querySelector(".header");
            if (window.scrollY > 50) {
                header.classList.add("scrolled");
            } else {
                header.classList.remove("scrolled");
            }
        });
    </script>

    <script src="~/MyJs/Layout.js"></script>
</body>
</html>
